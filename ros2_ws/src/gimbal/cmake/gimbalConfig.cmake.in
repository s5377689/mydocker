@PACKAGE_INIT@

find_dependency(rclcpp)
find_dependency(sensor_msgs)
find_dependency(custom_msgs)
find_dependency(OpenCV)
find_dependency(cv_bridge)

# Qt6 is optional - only needed for GCS components
find_dependency(Qt6 COMPONENTS Core QUIET)

include("${CMAKE_CURRENT_LIST_DIR}/gimbalTargets.cmake")

check_required_components(gimbal)

check_required_components(gimbal)
